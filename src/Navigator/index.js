import React from 'react';
import HomeNavigator from './HomeNavigator'
const AppNavigator = () => {
  // Here we can load the conditional pages like spash screen animation if needed
  // handle the Authentiocation flow etc ..
  return <HomeNavigator />
}
export default AppNavigator;
